defaults:
  - backbone: wgan-gp-in
  - _self_

name: wgan
channels: 3
generator_iters: 1000 # 100000
lambda_term: 1e-4
number_of_images: 100
use_checkpoint: false

training:
  epochs: 50
  batch_size: 128
  save_interval: 200
  device: cuda
  save_flag: false