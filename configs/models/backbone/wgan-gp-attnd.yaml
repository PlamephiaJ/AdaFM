# A4 注意力判别器（中层自注意力，无 SN/Norm）
# Tag：WGAN-GP-AttnD
# 用途：引入长程依赖的 D，检验极值动态下的稳定性。
# 注意：你的 discriminator_attention 代码里没有 IN；我们已把 SN 关了，符合 GP。

name: wgan-gp-attnd

generator:
  name: generator_resnet
  channels: 3
  in_dim: 50
  checkpoint_path: null
discriminator:
  name: discriminator_attention
  channels: 3
  use_spectral_norm: false
  checkpoint_path: null